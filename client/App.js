const createTodoItem = (event) => {
    event.preventDefault();
    const form = document.getElementById("createForm");

    const formData = new FormData(form);

    fetch()

    fetch("https")

    const text =
    const li = document.createElement('li');
    li.setAttribute('todo', text);
    
}